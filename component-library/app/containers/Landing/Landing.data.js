import base from '../../layouts/Base/Base.data';
import hero from '../../components/Hero/Hero.data';
import modules from '../../components/SectionListModule/SectionListModule.data';

const data = {
    ...base,
    breadcrumbs: null,
    hero: hero,
    modules: modules,
};

export default data;
