import cardNews from '../CardNews/CardNews.data';
import button from '../Button/Button.data';

const data = {
    title: 'Title for news list',
    items: [cardNews, cardNews, cardNews],
    button: button,
};

export default data;
