import React from 'react';
import __Container from './__Container';
import data from './__Container.data';

export default { title: '__StoryTitle' };

export const __ContainerDefault = () => (
    <__Container {...data} />
);
